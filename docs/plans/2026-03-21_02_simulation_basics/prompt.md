We need to sort out the basic simulation now, which we will layer complexity on to over time.

Read docs/design/overview.md and docs/design/simulation/simulation_overview.md to get an understanding of what we are trying to achieve.

For this first plan, we need to just get from 0 to 1.

Let's make it so that when a player clicks "New Career" it launches them directly to a "simulation debug" screen which displays:

1. a 24 hour clock that starts ticking from 0. This should be tied directly to an "in-game datetime" entity which the game ticks at real-time.
2. a list of People. This list starts empty.

On the first second of simulation, a Person entity should be generated by the simulation. The list of people on the screen should be updated because the person has come into existence. The person should be given a random first name and last name from a pool of random first and last names.

For now, that will be the entire simulation. The focus on this task is data structures and laying a very basic foundation: proving we can generate people, and reflect those people in the game UI. This will continue infinitely until the user closes the game.

I am keen to see how you structure the data with an eye to future siginficant complexity. I want your opinion on whether we scan safely implement this in GDscript, or whether we need to move to C# or C or C++ right out of the gate for the simulation aspects of the game.

I am also curious how you plan to maintain a data structure that allows us to maintain the entire simulation (at some point, potentially hundreds or low thousands of people at the same time, with thousands of locations, items, events, movement history, etc.).

Please write the plan.md as a sibling to this file, and I'll review before implementation.